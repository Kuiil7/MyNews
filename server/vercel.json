{
    "version": 2,
    "builds": [{ "src": "server.js", "use": "@now/node-server" }],
    "routes": [
      {
        "src": "src/server.js",
        "dest": "/server.js",
        "methods": ["GET"]
      },
      {
        "src": "/(.*)",
        "dest": "/server.js"

      }
    ]
  }